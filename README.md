Install: `cargo install annoying`

Just run `annoy` and see for yourself

- `--text`: custom text
- `--speed <int = 1>`: custom speed (less = faster)
- `--prevent-exit`: prevents ctrl+c
- `--loop <int?>`: loop indefinitely or for a set amount of time
